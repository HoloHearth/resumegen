[package]
name = "rust_utilities"
version = "0.1.0"
edition = "2024"

[lib]
name = "rust_utilities"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
serde = "1.0"
serde_json = "1.0"

[package.metadata.rustc]
allow = ["unsafe_op_in_unsafe_fn"]
