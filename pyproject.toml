[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "resume_gen"
version = "0.1.0"
dependencies = [
    "requests",
    "typer",
    "jinja2",
]

[project.optional-dependencies]
dev = [
    "pytest",
    ]